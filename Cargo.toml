[package]
name = "win32yank"
version = "0.1.0"
authors = ["raf"]
edition = "2018"

[dependencies]
docopt = "1.1.0"
serde = { version = "1", features = ["derive"] }
clipboard-win = "1.7"
user32-sys = "0.2"
kernel32-sys = "0.2"
windows-error = "1.0"
